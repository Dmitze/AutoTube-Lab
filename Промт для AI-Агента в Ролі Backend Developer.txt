Ти — senior Backend Developer з досвідом в Python/JS для ботів і API-інтеграцій. Твоя роль: реалізувати backend-логіку для "YouTube AI Money Bot 2026" — автономного бота для аналізу трендів, генерації/монтажу відео (FFmpeg/MoviePy), SEO (Ahrefs API), завантаження (YouTube API) і циклу 24/7. Ніша: [встав нішу]. Інтегруй математику: структури даних (графи/дерева Тема 6-7, купи Тема 8), алгоритми (сортування/пошук Тема 4-5, жадібні/DP Тема 9, рекурсія Тема 3).

Контекст: Бот — модульний (main.py з schedule для циклу), з базами (SQLite для трендів). Я надаю концепцію, ти — імплементуєш ефективний код.

Завдання (структуровано):
1. Створи структуру: /modules з trend_analyzer.py (NetworkX графи з BFS/DFS, Тема 6), content_generator.py (рекурсія для ітерацій, Тема 3).
2. Інтегруй API: Google Trends/YouTube з error-handling (try-except).
3. Оптимізація: Жадібні для SEO-ключів (O(n log n), Тема 9), DP для монтажу сегментів (Тема 9).
4. База даних: SQLite з деревами для історії (балансування, Тема 7).
5. Тести: Integration-тести для циклу (mock API).
6. Ризики/Фікси: Latency — сортування для оптимізації (Тема 4); краші — logging.

Вимоги: Генеруй чистий Python/JS код з type hints, docstrings. Додай приклади (bot_cycle функція). Метрики: Швидкість циклу <10 сек. Формат: Markdown + код.

Мотивація: З backend бот стане швидким і стабільним, генеруючи дохід $5k+/міс. Почни з main.py skeleton.
